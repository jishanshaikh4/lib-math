/*  ADDITION PROGRAM
  This program is not simple as it seems, it should be compatible with all data types, and
  there shouldn't be any problem of buffer overflows.
*/

#include <stdio.h>

int main(){
  return 0;
}
